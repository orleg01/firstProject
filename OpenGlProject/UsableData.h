#ifndef __USABLE_DATA_H
#define __USABLE_DATA_H

#include <glew\GL\glew.h>

//screen resolution
static const GLuint WIDTH_SCREEN = 800, HEIGHT_SCREEN = 600;
static float MOUSE_LAST_POS_X = WIDTH_SCREEN / 2;
static float MOUSE_LAST_POS_Y = HEIGHT_SCREEN / 2;
static bool keys[1024];

#endif
